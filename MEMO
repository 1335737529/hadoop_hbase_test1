
var dateReg = /^(\d{4})-(\d{2})-(\d{2})$/;
 
function verifyDate(dateStr, dateReg){
    //日期格式不匹配
    if( !dateReg.test(dateStr) ){
        return false;
    }
 
    //使用捕获组获取日期
    var date = {
        year : RegExp.$1,
        month : RegExp.$2,
        day : RegExp.$3,
    }
 
    //使用 Date() 对象，新建对象时会将日期转化为合法日期
    //比如 2020-02-30 被转化为 2020-3-1
    var dateObj = new Date(date.year, date.month-1, date.day);
 
    if( date.year != dateObj.getFullYear() || date.month != dateObj.getMonth()+1 || date.day != dateObj.getDate() ){
        return false;
    }
    return true;
休暇を以下の日程でお願いしたく思います：
休暇開始日：〇〇年〇月〇日（○曜日）
休暇終了日：〇〇年〇月〇日（○曜日）

この期間中、業務の引き継ぎや対応等につきましては、万全を期すよう努めますので、何卒ご了承いただけますと幸いです。

何かご不明点やご提案等ございましたら、お知らせください。また、何か臨時の対応が必要な場合には、遠慮なくご連絡ください。

急なお願いとなりますが、ご検討の程、よろしくお願い申し上げます。

お忙しいところ恐れ入りますが、何卒よろしくお願い申し上げます。

敬具
〇〇〇〇（あなたの名前）

以上です。

印鑑の画像のキャッシュに関連する問題が発生しています。CPログイン画面からデータにアクセスする場合は、その画面内で操作するのが最善です。2つのシステム間で印鑑を操作すると競合が発生する可能性があります。また、今日は5時に仕事を終える予定です。会社の忘年会があるため、何か問題があればアジャイルのメールボックスに送信してください。
}
